import { createStore } from 'vuex'

import menuModule from './modules/menuModule'

export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    menu: menuModule
  }
})
